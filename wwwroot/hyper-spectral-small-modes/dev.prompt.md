1、现在我已经把数据集都下载完整了
2、需要当前/Users/jiangshengping/wwwroot/shenzhen/spectrum/wwwroot/hyper-spectral-small-modes 目录中，整理出每个模型内容，目录入下：source 放源代码，docs放文档，datasets放测试数据集

---

## 执行结果（2026-07-30）

已完成：共整理 **87** 个模型，结构为：

```text
YYYY_ModelName/
  source/      # 源代码
  docs/        # 文档
  datasets/    # -> ../datasets（共享数据软链）
```

共享数据在根目录 `datasets/`。  
说明见根目录 `README.md`。

备注：`Houston2013` 仍为空，`MUUFL` 尚未放入共享 datasets。
