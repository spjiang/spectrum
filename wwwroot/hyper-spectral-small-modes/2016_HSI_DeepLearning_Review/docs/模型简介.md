# HSI_DeepLearning_Review

对应 2016 年 CNN 高光谱分类论文的可运行实现与基线对比代码，而非纯文字综述。

`source/` 提供：`cnn1d` / `cnn2d` / `cnn3d`、MLP、RNN、SVM、随机森林、多项逻辑回归，以及跨数据集迁移与 ImageNet 预训练迁移。输入为高光谱立方体（可 PCA + 标准化），按像素光谱或空间 patch 做**像素级地物分类**，输出 OA/AA/Kappa 与类别预测。支持 Indian Pines、PaviaU、Salinas、Houston。
